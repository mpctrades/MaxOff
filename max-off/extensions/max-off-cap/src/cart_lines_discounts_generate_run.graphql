query CartInput {
  cart {
    cost {
      subtotalAmount {
        amount
        currencyCode
      }
    }
  }
  discount {
    discountClasses
    metafield(namespace: "$app", key: "cap_config") {
      jsonValue
    }
  }
}
