# Deployed to /etc/nginx/maxoff/dev-active-upstream.conf.
#
# The live colour, and nothing else. deploy/deploy.sh rewrites this one line
# and reloads nginx; this copy is the starting point and the record of the
# format. 3010 is blue, 3011 is green.
server 127.0.0.1:3010;
